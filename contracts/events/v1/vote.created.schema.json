{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "vote.created",
  "type": "object",
  "required": [
    "event_id",
    "event_type",
    "occurred_at",
    "source_service",
    "schema_version",
    "partition_key",
    "data"
  ],
  "properties": {
    "event_id": { "type": "string" },
    "event_type": { "const": "vote.created" },
    "occurred_at": { "type": "string", "format": "date-time" },
    "source_service": { "type": "string" },
    "schema_version": { "type": "string" },
    "partition_key": { "type": "string" },
    "data": {
      "type": "object",
      "required": ["vote_id", "submission_id", "campaign_id", "user_id", "vote_type"],
      "properties": {
        "vote_id": { "type": "string" },
        "submission_id": { "type": "string" },
        "campaign_id": { "type": "string" },
        "user_id": { "type": "string" },
        "vote_type": { "type": "string" }
      }
    }
  }
}

